import { CardContent, styled } from '@mui/material';

export const MobileEmergencyContacts = styled(CardContent)(({ theme }) => ({
	display: 'flex',
	flexDirection: 'column',
	alignItems: 'center',
	gap: '0.75rem',
}));
