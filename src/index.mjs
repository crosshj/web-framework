const Button = () => {};

const NewComponent = () => {};

const Tables = () => {};

const ScreenOptions = () => {};

const Layouts = () => {};

const Widgets = () => {};

const Component = () => {};

export const components = {
	Button,
	NewComponent,
	Tables,
};

export const screenSelector = {
	ScreenOptions,
	Layouts,
	Widgets,
};

export const selector = {
	Component,
};

export const composer = {
	// ??
};

export const screen = {
	// ??
};

/* 
export const layout = {
	Title, 
	Description, 
	Tables, 
	TableSelector
} = Widgets

*/
