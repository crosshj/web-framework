# @awoss/web

## How to use

```javascript
import {
	components,
	screenSelector,
	selector,
	layout,
	composer,
	screen,
} from '@awoss/web';

const { Button, NewComponent, Tables } = components;
const { ScreenOptions, Layouts, Widgets } = screenSelector;
const { Component } = selector;
const {} = composer;
const {} = layout;
const {} = screen;

console.log(components);
console.log(Button);
console.log(NewComponent);
console.log(Tables);
console.log(ScreenOptions);
console.log(Layouts);
console.log(Widgets);
console.log(Component);
```
